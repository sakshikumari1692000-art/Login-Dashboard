👉 //File Structure
src/
├── api/
│ └── axiosInstance.ts # Axios setup with JWT interceptor
│ └── booksApi.ts # Book-related API calls
├── auth/
│ └── AuthContext.tsx # React Context for JWT auth state
│ └── ProtectedRoute.tsx # Route guard component
├── components/
│ ├── Navbar.tsx # Top navigation bar
│ ├── BookCard.tsx # Individual book card
│ ├── BookList.tsx # Grid of book cards
│ ├── Sidebar.tsx # Dashboard sidebar
│ └── SearchBar.tsx # Search/filter bar
├── pages/
│ ├── LoginPage.tsx # Login form
│ ├── SignupPage.tsx # Signup form
│ ├── Dashboard.tsx # Main dashboard layout
│ ├── BookDetails.tsx # Single book detail view
│ └── NotFound.tsx # 404 page
├── types/
│ └── index.ts # TypeScript interfaces
├── App.tsx
├── main.tsx
├── App.css
└── index.css


👉 Dashboard Features You Should Build

Feature Description
Sidebar Genre filter, navigation links (Home, Favorites, Profile)
Navbar Search bar, user avatar/name, logout button
BookCard Cover image, title, author, price, rating stars, "View Details" button
Stats Row Total books, genres count, top-rated book
BookDetails Page Full book info, related books, "Add to Cart" button
Search Filter books by title, author, or genre
Dark Mode Toggle Switch between light/dark themes


👉 Free Dummy Book APIs You Can Use
API URL Auth?
Open Library https://openlibrary.org/search.json?q=the+lord+of+the+rings No
Google Books https://www.googleapis.com/books/v1/volumes?q=javascript No
IT Bookstore https://api.itbook.store/1.0/new No
Fake Store https://fakestoreapi.com/products/category/books No

Step 1: Install Required Packages

npm install react-router-dom axios jwt-decode
npm install -D @types/jwt-decode

Step 2: TypeScript Types — src/types/index.ts

export interface User {
id: string;
email: string;
name: string;
}

export interface AuthState {
user: User | null;
token: string | null;
isAuthenticated: boolean;
}

export interface LoginCredentials {
email: string;
password: string;
}

export interface SignupCredentials {
name: string;
email: string;
password: string;
}

export interface Book {
id: number;
title: string;
author: string;
genre: string;
description: string;
image: string;
price: number;
rating: number;
}

👉 Step 3: JWT Auth Context — src/auth/AuthContext.tsx

import { createContext, useContext, useState, useEffect, ReactNode } from "react";
import { AuthState, User, LoginCredentials, SignupCredentials } from "../types";

interface AuthContextType extends AuthState {
  login: (credentials: LoginCredentials) => Promise<void>;
  signup: (credentials: SignupCredentials) => Promise<void>;
  logout: () => void;
}

const AuthContext = createContext<AuthContextType | undefined>(undefined);

const USERS_STORAGE_KEY = "bookstore_users"; // stored registered users
const TOKEN_KEY = "token";

// Stored user shape in localStorage
interface StoredUser {
  id: string;
  name: string;
  email: string;
  password: string; // In real app, NEVER store plain text passwords
}

// Get all registered users from localStorage
const getStoredUsers = (): StoredUser[] => {
  const data = localStorage.getItem(USERS_STORAGE_KEY);
  return data ? JSON.parse(data) : [];
};

// Save users to localStorage
const saveUsers = (users: StoredUser[]): void => {
  localStorage.setItem(USERS_STORAGE_KEY, JSON.stringify(users));
};

// Fake JWT token generator
const fakeJWT = (user: User): string => {
  const header = btoa(JSON.stringify({ alg: "HS256", typ: "JWT" }));
  const payload = btoa(
    JSON.stringify({ ...user, exp: Date.now() + 3600000 })
  );
  return `${header}.${payload}.fake-signature`;
};

// Decode token
const decodeToken = (token: string): User | null => {
  try {
    const payload = JSON.parse(atob(token.split(".")[1]));
    if (payload.exp < Date.now()) return null;
    return { id: payload.id, email: payload.email, name: payload.name };
  } catch {
    return null;
  }
};

export const AuthProvider = ({ children }: { children: ReactNode }) => {
  const [authState, setAuthState] = useState<AuthState>({
    user: null,
    token: null,
    isAuthenticated: false,
  });

  // Check token on mount
  useEffect(() => {
    const token = localStorage.getItem(TOKEN_KEY);
    if (token) {
      const user = decodeToken(token);
      if (user) {
        setAuthState({ user, token, isAuthenticated: true });
      } else {
        localStorage.removeItem(TOKEN_KEY);
      }
    }
  }, []);

  // SIGNUP — stores user data in localStorage
  const signup = async (credentials: SignupCredentials): Promise<void> => {
    const users = getStoredUsers();

    // Check if email already exists
    const existingUser = users.find((u) => u.email === credentials.email);
    if (existingUser) {
      throw new Error("Email already registered");
    }

    // Create new user and store it
    const newUser: StoredUser = {
      id: Date.now().toString(),
      name: credentials.name,
      email: credentials.email,
      password: credentials.password,
    };

    users.push(newUser);
    saveUsers(users);

    // Auto-login after signup
    const user: User = { id: newUser.id, name: newUser.name, email: newUser.email };
    const token = fakeJWT(user);
    localStorage.setItem(TOKEN_KEY, token);
    setAuthState({ user, token, isAuthenticated: true });
  };

  // LOGIN — checks credentials against stored users
  const login = async (credentials: LoginCredentials): Promise<void> => {
    const users = getStoredUsers();

    // Find user by email
    const foundUser = users.find((u) => u.email === credentials.email);

    if (!foundUser) {
      throw new Error("No account found with this email. Please sign up first.");
    }

    // Check password
    if (foundUser.password !== credentials.password) {
      throw new Error("Incorrect password");
    }

    // Generate token and login
    const user: User = { id: foundUser.id, name: foundUser.name, email: foundUser.email };
    const token = fakeJWT(user);
    localStorage.setItem(TOKEN_KEY, token);
    setAuthState({ user, token, isAuthenticated: true });
  };

  // LOGOUT
  const logout = () => {
    localStorage.removeItem(TOKEN_KEY);
    setAuthState({ user: null, token: null, isAuthenticated: false });
  };

  return (
    <AuthContext.Provider value={{ ...authState, login, signup, logout }}>
      {children}
    </AuthContext.Provider>
  );
};

export const useAuth = (): AuthContextType => {
  const context = useContext(AuthContext);
  if (!context) throw new Error("useAuth must be used within AuthProvider");
  return context;
};


👉Step 4: Protected Route — src/auth/ProtectedRoute.tsx

                    import { Navigate } from "react-router-dom";
                    import { useAuth } from "./AuthContext";
                    import { ReactNode } from "react";

                    const ProtectedRoute = ({ children }: { children: ReactNode }) => {
                    const { isAuthenticated } = useAuth();
                    return isAuthenticated ? <>{children}</> :
                    <Navigate to="/login" replace />;
                    };

                    export default ProtectedRoute;

👉Step 5: Axios with JWT Interceptor — src/api/axiosInstance.ts

                    import axios from "axios";

                    const axiosInstance = axios.create({
                    baseURL: "https://api.example.com", // Replace with real API
                    });

                    // Attach JWT token to every request
                    axiosInstance.interceptors.request.use((config) => {
                    const token = localStorage.getItem("token");
                    if (token) {
                    config.headers.Authorization = `Bearer ${token}`;
                    }
                    return config;
                    });

                    // Handle 401 responses (expired/invalid token)
                    axiosInstance.interceptors.response.use(
                    (response) => response,
                    (error) => {
                    if (error.response?.status === 401) {
                    localStorage.removeItem("token");
                    window.location.href = "/login";
                    }
                    return Promise.reject(error);
                    }
                    );

                    export default axiosInstance;

 👉 Step 6: Books API with Dummy Data — src/api/booksApi.ts

                    import { Book } from "../types";

                    // Using Open Library API (free, no auth needed)
                    // Or use this local dummy data as fallback

                    const dummyBooks: Book[] = [
                    { id: 1, title: "The Great Gatsby", author: "F. Scott Fitzgerald", genre: "Classic", description: "A
                    novel about the American dream.", image: "https://covers.openlibrary.org/b/id/7222246-L.jpg", price:
                    12.99, rating: 4.5 },
                    { id: 2, title: "To Kill a Mockingbird", author: "Harper Lee", genre: "Fiction", description: "A
                    story of racial injustice.", image: "https://covers.openlibrary.org/b/id/8228691-L.jpg", price:
                    10.99, rating: 4.8 },
                    { id: 3, title: "1984", author: "George Orwell", genre: "Dystopian", description: "A dystopian
                    social science fiction.", image: "https://covers.openlibrary.org/b/id/7222246-L.jpg", price: 9.99,
                    rating: 4.7 },
                    { id: 4, title: "Pride and Prejudice", author: "Jane Austen", genre: "Romance", description: "A
                    romantic novel.", image: "https://covers.openlibrary.org/b/id/8091016-L.jpg", price: 8.99, rating:
                    4.6 },
                    { id: 5, title: "The Hobbit", author: "J.R.R. Tolkien", genre: "Fantasy", description: "A fantasy
                    adventure.", image: "https://covers.openlibrary.org/b/id/6979861-L.jpg", price: 14.99, rating: 4.9
                    },
                    { id: 6, title: "Moby Dick", author: "Herman Melville", genre: "Adventure", description: "A
                    whale-hunting adventure.", image: "https://covers.openlibrary.org/b/id/7222246-L.jpg", price: 11.99,
                    rating: 4.2 },
                    ];

                    export const fetchBooks = async (): Promise<Book[]> => {
                        // Simulate network delay
                        return new Promise((resolve) => {
                        setTimeout(() => resolve(dummyBooks), 800);
                        });
                        };

                        export const fetchBookById = async (id: number): Promise<Book | undefined> => {
                            return new Promise((resolve) => {
                            setTimeout(() => resolve(dummyBooks.find((b) => b.id === id)), 500);
                            });
                            };

                            export const searchBooks = async (query: string): Promise<Book[]> => {
                                return new Promise((resolve) => {
                                setTimeout(() => {
                                const filtered = dummyBooks.filter(
                                (b) =>
                                b.title.toLowerCase().includes(query.toLowerCase()) ||
                                b.author.toLowerCase().includes(query.toLowerCase()) ||
                                b.genre.toLowerCase().includes(query.toLowerCase())
                                );
                                resolve(filtered);
                                }, 300);
                                });
                                };


👉Step 7: App Routing — src/App.tsx

                                import { BrowserRouter, Routes, Route, Navigate } from "react-router-dom";
                                import { AuthProvider } from "./auth/AuthContext";
                                import ProtectedRoute from "./auth/ProtectedRoute";
                                import LoginPage from "./pages/LoginPage";
                                import SignupPage from "./pages/SignupPage";
                                import Dashboard from "./pages/Dashboard";
                                import BookDetails from "./pages/BookDetails";

                                function App() {
                                return (
                                <AuthProvider>
                                    <BrowserRouter>
                                        <Routes>
                                            <Route path="/login" element={<LoginPage />} />
                                            <Route path="/signup" element={<SignupPage />} />
                                            <Route path="/dashboard" element={ <ProtectedRoute>
                                                <Dashboard />
                                                </ProtectedRoute>
                                                }
                                                />
                                                <Route path="/book/:id" element={ <ProtectedRoute>
                                                    <BookDetails />
                                                    </ProtectedRoute>
                                                    }
                                                    />
                                                    <Route path="*" element={<Navigate to="/login" />} />
                                        </Routes>
                                    </BrowserRouter>
                                </AuthProvider>
                                );
                                }

                                export default App;

👉Step 8: Dashboard Page Idea — src/pages/Dashboard.tsx

                                import { useEffect, useState } from "react";
                                import { useAuth } from "../auth/AuthContext";
                                import { fetchBooks, searchBooks } from "../api/booksApi";
                                import { Book } from "../types";
                                import BookCard from "../components/BookCard";
                                import Navbar from "../components/Navbar";
                                import Sidebar from "../components/Sidebar";

                                const Dashboard = () => {
                                const { user } = useAuth();
                                const [books, setBooks] = useState<Book[]>([]);
                                    const [loading, setLoading] = useState(true);
                                    const [searchQuery, setSearchQuery] = useState("");
                                    const [activeGenre, setActiveGenre] = useState("All");

                                    useEffect(() => {
                                    loadBooks();
                                    }, []);

                                    const loadBooks = async () => {
                                    setLoading(true);
                                    const data = await fetchBooks();
                                    setBooks(data);
                                    setLoading(false);
                                    };

                                    const handleSearch = async (query: string) => {
                                    setSearchQuery(query);
                                    if (query.trim() === "") {
                                    loadBooks();
                                    } else {
                                    const results = await searchBooks(query);
                                    setBooks(results);
                                    }
                                    };

                                    const filteredBooks =
                                    activeGenre === "All"
                                    ? books
                                    : books.filter((b) => b.genre === activeGenre);

                                    const genres = ["All", ...new Set(books.map((b) => b.genre))];

                                    return (
                                    <div className="dashboard-layout">
                                        <Sidebar genres={genres} activeGenre={activeGenre}
                                            onGenreSelect={setActiveGenre} />
                                        <div className="dashboard-main">
                                            <Navbar userName={user?.name || "" } onSearch={handleSearch} />
                                            <div className="dashboard-content">
                                                <h2>Welcome back, {user?.name} 📚</h2>
                                                <div className="stats-row">
                                                    <div className="stat-card">📖 {books.length} Books</div>
                                                    <div className="stat-card">🏷️ {genres.length - 1} Genres</div>
                                                    <div className="stat-card">⭐ Top Rated</div>
                                                </div>
                                                {loading ? (
                                                <div className="loading-spinner">Loading...</div>
                                                ) : (
                                                <div className="book-grid">
                                                    {filteredBooks.map((book) => (
                                                    <BookCard key={book.id} book={book} />
                                                    ))}
                                                </div>
                                                )}
                                            </div>
                                        </div>
                                    </div>
                                    );
                                    };

                                    export default Dashboard;

step 9: Login page 
import { useState, FormEvent } from "react";
import { useNavigate, Link } from "react-router-dom";
import { useAuth } from "../auth/AuthContext";

const LoginPage = () => {
  const [email, setEmail] = useState("");
  const [password, setPassword] = useState("");
  const [error, setError] = useState("");
  const [loading, setLoading] = useState(false);
  const { login } = useAuth();
  const navigate = useNavigate();

  const handleSubmit = async (e: FormEvent) => {
    e.preventDefault();
    setError("");

    if (!email || !password) {
      setError("Please fill in all fields");
      return;
    }

    try {
      setLoading(true);
      await login({ email, password });
      navigate("/dashboard");
    } catch (err: any) {
      setError(err.message || "Login failed");
    } finally {
      setLoading(false);
    }
  };

  return (
    <div className="auth-container">
      <div className="auth-card">
        <h1>📚 BookStore</h1>
        <h2>Welcome Back</h2>
        <p className="auth-subtitle">Sign in to your account</p>

        {error && <div className="auth-error">{error}</div>}

        <form onSubmit={handleSubmit}>
          <div className="form-group">
            <label htmlFor="email">Email</label>
            <input id="email" type="email" value={email} onChange={(e) => setEmail(e.target.value)} />
          </div>

          <div className="form-group">
            <label htmlFor="password">Password</label>
            <input id="password" type="password" value={password} onChange={(e) => setPassword(e.target.value)} />
          </div>

          <button type="submit" className="auth-button" disabled={loading}>
            {loading ? "Signing in..." : "Sign In"}
          </button>
        </form>

        <p className="auth-footer">
          Don't have an account? <Link to="/signup">Sign Up</Link>
        </p>
      </div>
    </div>
  );
};

export default LoginPage;

step 10: Signup page 

import { useState, FormEvent } from "react";
import { useNavigate, Link } from "react-router-dom";
import { useAuth } from "../auth/AuthContext";

const SignupPage = () => {
  const [name, setName] = useState("");
  const [email, setEmail] = useState("");
  const [password, setPassword] = useState("");
  const [confirmPassword, setConfirmPassword] = useState("");
  const [error, setError] = useState("");
  const [loading, setLoading] = useState(false);
  const { signup } = useAuth();
  const navigate = useNavigate();

  const handleSubmit = async (e: FormEvent) => {
    e.preventDefault();
    setError("");

    if (!name || !email || !password || !confirmPassword) {
      setError("Please fill in all fields");
      return;
    }

    if (password !== confirmPassword) {
      setError("Passwords do not match");
      return;
    }

    if (password.length < 6) {
      setError("Password must be at least 6 characters");
      return;
    }

    try {
      setLoading(true);
      await signup({ name, email, password });
      navigate("/dashboard");
    } catch (err: any) {
      setError(err.message || "Signup failed");
    } finally {
      setLoading(false);
    }
  };

  return (
    <div className="auth-container">
      <div className="auth-card">
        <h1>📚 BookStore</h1>
        <h2>Create Account</h2>
        <p className="auth-subtitle">Join our bookstore community</p>

        {error && <div className="auth-error">{error}</div>}

        <form onSubmit={handleSubmit}>
          <div className="form-group">
            <label htmlFor="name">Full Name</label>
            <input id="name" type="text" value={name} onChange={(e) => setName(e.target.value)} />
          </div>

          <div className="form-group">
            <label htmlFor="email">Email</label>
            <input id="email" type="email" value={email} onChange={(e) => setEmail(e.target.value)} />
          </div>

          <div className="form-group">
            <label htmlFor="password">Password</label>
            <input id="password" type="password" value={password} onChange={(e) => setPassword(e.target.value)} />
          </div>

          <div className="form-group">
            <label htmlFor="confirmPassword">Confirm Password</label>
            <input id="confirmPassword" type="password" value={confirmPassword} onChange={(e) => setConfirmPassword(e.target.value)} />
          </div>

          <button type="submit" className="auth-button" disabled={loading}>
            {loading ? "Creating Account..." : "Sign Up"}
          </button>
        </form>

        <p className="auth-footer">
          Already have an account? <Link to="/login">Sign In</Link>
        </p>
      </div>
    </div>
  );
};

export default SignupPage;
